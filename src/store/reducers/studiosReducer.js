const initialState = {
  studios: [],
  error: null,
  isFetching: false
}

export default function studiosReducer(state = initialState, {type, payload}){
  switch(type){
    default: return state;
  }
}